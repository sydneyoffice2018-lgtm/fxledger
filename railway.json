{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "node --import tsx/esm server/index.ts",
    "healthcheckPath": "/",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE"
  }
}
